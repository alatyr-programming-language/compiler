## P2 ambient-injection probe: a minimal, non-generic, lean-lowerable stdlib module. Referencing
## `std::probe::answer()` must ambiently inject this file (module "std__probe") with no manual listing.
pub answer := fn() -> u64 { return 42 }
