## The ANCESTOR's declarations — nameable from the whole subtree (§3), but SHADOWED wherever a
## descendant declares the same name.
Box := struct { a : u64, b : u64 }               ## 16
E := enum { A(u64), B(u64) }                     ## 16
