## A DECOY: unrelated, non-`pub`, unnameable from `geo`s subtree (§3), WIDER than every
## declaration on the chain so resolving here is a distinct wrong value.
Box := struct { a : u64, b : u64, c : u64, d : u64 }    ## 32
E := enum { A(u64, u64, u64), B }                        ## 32
