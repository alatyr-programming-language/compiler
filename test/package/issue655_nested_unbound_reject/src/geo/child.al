## A NESTED submodule of this package (`geo::child`, mangled `geo__child`). The name below is
## declared by no module in the compile list, no ancestor, and no library; the reject must name
## THIS module, so a checker that skipped the file would let the whole package build.
pub run := fn() -> u64 { return zzz_not_declared_anywhere }
